<!DOCTYPE html>
<html lang="en">

<%- include('../partials/head') %>

<body>
    
    <%- include('../partials/header') %>

    <main>
        <section class="seccion-perfil-usuario">
            <div class="perfil-usuario-header">
                <div class="perfil-usuario-portada">
                    <h1>Perfil de Usuario</h1>
                    <div class="perfil-usuario-avatar">
                        <img src="/img/avatars/<%= user.avatar %>" alt="imagen de usuario">
                    </div>
                </div>
            </div>
            <div class="perfil-usuario-body">
                <div class="perfil-usuario-bio">
                    <h3 class="titulo"><%= user.first_name %> <%= user.last_name%></h3>
                    <p class="texto">Hola , ¡Bienvenido a i_MENTOR!. Es para nosotros un placer que te hayas unido a esta comunidad sin igual.</p>
                </div>
                <div class="perfil-usuario-footer">
                    <ul class="lista-datos">
                        <li><i class="icono fas fa-map-signs"></i>E-mail: <%= user.email%></li>
                    </ul>
                    <ul class="lista-datos">
                        <li><i class="icono fas fa-user-check"></i>Nombre de usuario: <%= user.user_name%></li>
                    </ul>
                </div>
                <div class="opciones">
                    <a href="" class="boton-opciones editar"><i class="fas fa-pen"></i></a>
                    <a href="/products/productCart" class="boton-opciones cesta-compras"><i class="fas fa-shopping-basket"></i></a>
                    <a href="/paginaEnConstruccion" class="boton-opciones plataforma"><i class="fas fa-hand-pointer"></i></a>
                </div>
                <form action="/users/editPW/<%= user.user_id%>/<%= user.email%>?_method=PUT" method="POST" class="password-form" >
                    <div class="registerbox-pw">
                        <label for="password_old"><i class="fas fa-lock"></i> Ingrese su contraseña anterior</label>
                        <div class="box-showpassword">
                            <input class="boxes" type="password" name="password_old" value="" id="password_old" placeholder="Contraseña">
                            <div class="show-password">
                                <input type="checkbox" name="show-password"> Mostrar Contraseña
                            </div>
                        </div>
                        <% if (locals.errors && errors.password_old) { %>
                        <div class="text-danger">
                            <p class="feedback"><%= errors.password_old.msg %></p>
                        </div>
                        <% } %>
                    </div>
                    <div class="registerbox-pw">
                        <label for="password"><i class="fas fa-lock"></i> Ingrese su nueva contraseña</label>
                        <div class="box-showpassword">
                            <input class="boxes" type="password" name="password" value="" id="password"
                                placeholder="Contraseña">
                            <div class="show-password">
                                <input type="checkbox" name="show-password" onclick="myFunction()"> Mostrar Contraseña
                                <script>
                                    function myFunction() {
                                        var x = document.getElementById("password");
                                        if (x.type === "password") {
                                            x.type = "text";
                                        } else {
                                            x.type = "password";
                                        }
                                    }
                                </script>
                            </div>
                        </div>
                        <% if (locals.errors && errors.password) { %>
                        <div class="text-danger">
                            <p class="feedback"><%= errors.password.msg %></p>
                        </div>
                        <% } %>
                    </div>  
                    <div class="botones">
                        <button class="register-button" type="submit"><i class="fas fa-exclamation-triangle"></i> Modificar</button>
                        <button class="register-button" type="reset"><i class="fas fa-eraser"></i> Borrar</button>   
                    </div>
                </form>
                <div class="boton-rojo">
                    <a href="/users/profile">Cancelar</a>
                </div>
            </div>
        </section>  
    </main>
    <%- include('../partials/footer') %>
</body>
</html>